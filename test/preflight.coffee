#chrome =
#  notifications:
#    onClicked:
#      addListener: (cb) ->
#    onShowSettings:
#      addListener: (cb) ->
#    onButtonClicked:
#      addListener: (cb) ->
#  runtime:
#    onMessage:
#      addListener: (cb) ->
#  tabs:
#    create: ->